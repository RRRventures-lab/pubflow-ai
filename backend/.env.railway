# ============================================================================
# PubFlow AI Backend - Railway Production Environment Variables
# Configure these in Railway dashboard under Variables
# ============================================================================

# --------------------------------------------------------------------------
# Required - Set in Railway
# --------------------------------------------------------------------------
NODE_ENV=production
PORT=${{PORT}}

# Database (auto-set by Railway when you add PostgreSQL)
DATABASE_URL=${{DATABASE_URL}}

# Redis (auto-set by Railway when you add Redis)
REDIS_URL=${{REDIS_URL}}

# JWT Secrets - Generate with: openssl rand -hex 32
JWT_SECRET=<generate-32-char-random>
JWT_REFRESH_SECRET=<generate-32-char-random>

# OpenAI API Key
OPENAI_API_KEY=sk-...

# --------------------------------------------------------------------------
# Optional - Defaults work for MVP
# --------------------------------------------------------------------------
# CORS_ORIGIN=https://your-frontend-domain.vercel.app
# LOG_LEVEL=info

# --------------------------------------------------------------------------
# Notes for Railway Setup
# --------------------------------------------------------------------------
# 1. Add PostgreSQL plugin (auto-sets DATABASE_URL)
# 2. Add Redis plugin (auto-sets REDIS_URL)
# 3. Enable pgvector extension in PostgreSQL:
#    - Connect to DB via Railway shell
#    - Run: CREATE EXTENSION vector;
# 4. Set the above required variables in Railway dashboard
# 5. Run migrations after first deploy:
#    - railway run npm run db:migrate
#    - railway run npm run db:seed
